{
  "name": "nx-workspace",
  "version": "independent",
  "license": "MIT",
  "scripts": {
    "build:all": "nx run-many -t build",
    "build:since": "nx affected -t build",
    "test:all": "nx run-many -t test",
    "test:since": "nx affected -t test",
    "dev": "nx run-many -t dev",
    "serve": "nx run-many -t serve --parallel=8"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^7.2.0",
    "nx": "16.8.1",
    "nx-cloud": "latest"
  },
  "workspaces": [
    "packages/*"
  ],
  "volta": {
    "node": "18.17.1"
  }
}
